<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>리그 정보</h1>
    <ul>
        <% leagues.forEach(league => { %>
            <li><%= league.league.name %> - <%= league.league.type %></li>
        <% }); %>
    </ul>
</body>
</html>